import { css } from '@emotion/css';
import { token } from '@atlaskit/tokens';

export const serverNameFormOuterContainer = css`
	border: 2px solid #dee0e4;
	margin-top: ${token('space.400')};
	width: 580px;
`;

export const serverNameForm = css`
	margin-top: ${token('space.300')};
`;

export const serverNameFormButton = css`
	margin-top: ${token('space.300')};
`;
